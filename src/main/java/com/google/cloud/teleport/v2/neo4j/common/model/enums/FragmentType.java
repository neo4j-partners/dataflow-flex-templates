package com.google.cloud.teleport.v2.neo4j.common.model.enums;

/**
 * Clauses in target mappings for nodes and relationships (soruce, target, rel).
 */
public enum FragmentType {
    source,
    target,
    rel,
    node
}
